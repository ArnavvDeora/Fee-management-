<UserControl x:Class="SchoolFeeSystem.Presentation.Views.MainSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#ECF0F1">
    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="Welcome, Admin" FontSize="32" FontWeight="Bold" 
                       HorizontalAlignment="Center" Margin="0,0,0,50" Foreground="#2C3E50"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Command="{Binding OpenFeeManagementCommand}" Width="300" Height="200" 
                        Background="White" BorderBrush="#27AE60" BorderThickness="0,0,0,5" Margin="0,0,20,0">
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="🎓" FontSize="50" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                        <TextBlock Text="Fee Management" FontSize="20" FontWeight="Bold" Foreground="#27AE60"/>
                        <TextBlock Text="Students, Fees, Reports" Foreground="Gray" Margin="0,5,0,0"/>
                    </StackPanel>
                </Button>

                <Button Command="{Binding OpenPayrollManagementCommand}" Width="300" Height="200" 
                        Background="White" BorderBrush="#C0392B" BorderThickness="0,0,0,5">
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="💼" FontSize="50" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                        <TextBlock Text="HR &amp; Payroll" FontSize="20" FontWeight="Bold" Foreground="#C0392B"/>
                        <TextBlock Text="Staff, Salaries, Payslips" Foreground="Gray" Margin="0,5,0,0"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>