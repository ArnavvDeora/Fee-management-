<UserControl x:Class="SchoolFeeSystem.Presentation.Views.SalarySettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#F8F9FA">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <Button Command="{Binding GoBackCommand}" Content="⬅ Back" HorizontalAlignment="Left" Background="Transparent" Foreground="#7F8C8D" BorderThickness="0" FontWeight="Bold"/>
            <TextBlock Text="Salary Configuration" FontSize="28" FontWeight="Bold" Foreground="#2C3E50"/>
            <TextBlock Text="Define global salary rules (e.g., HRA = 40% of Basic)." Foreground="#7F8C8D"/>
        </StackPanel>

        <Border Grid.Row="1" Background="White" CornerRadius="10" Padding="20" Margin="0,0,0,20">
            <StackPanel Orientation="Horizontal">
                <StackPanel Margin="0,0,15,0">
                    <TextBlock Text="Component Name" FontSize="12" Foreground="#7F8C8D"/>
                    <TextBox Text="{Binding NewName}" Width="150" Padding="5"/>
                </StackPanel>

                <StackPanel Margin="0,0,15,0">
                    <TextBlock Text="Type" FontSize="12" Foreground="#7F8C8D"/>
                    <ComboBox SelectedItem="{Binding NewType}" Width="100" Padding="5">
                        <ComboBoxItem Content="Earning"/>
                        <ComboBoxItem Content="Deduction"/>
                    </ComboBox>
                </StackPanel>

                <StackPanel Margin="0,0,15,0">
                    <TextBlock Text="Calculation" FontSize="12" Foreground="#7F8C8D"/>
                    <ComboBox SelectedItem="{Binding NewCalcType}" Width="120" Padding="5">
                        <ComboBoxItem Content="Fixed"/>
                        <ComboBoxItem Content="Percentage"/>
                    </ComboBox>
                </StackPanel>

                <StackPanel Margin="0,0,15,0">
                    <TextBlock Text="Value (% or ₹)" FontSize="12" Foreground="#7F8C8D"/>
                    <TextBox Text="{Binding NewValue}" Width="80" Padding="5"/>
                </StackPanel>

                <Button Command="{Binding AddComponentCommand}" Content="Add Rule" Background="#3498DB" Foreground="White" BorderThickness="0" FontWeight="Bold" Padding="20,0" Height="35" VerticalAlignment="Bottom">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Border>

        <DataGrid Grid.Row="2" ItemsSource="{Binding Components}" AutoGenerateColumns="False" 
                  CanUserAddRows="False" IsReadOnly="True" HeadersVisibility="Column" Background="White" BorderThickness="0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="*"/>
                <DataGridTextColumn Header="Logic" Binding="{Binding CalculationType}" Width="*"/>
                <DataGridTextColumn Header="Value" Binding="{Binding Value}" Width="*"/>
                <DataGridTemplateColumn Header="Action" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="🗑 Remove" Command="{Binding DataContext.DeleteComponentCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                    CommandParameter="{Binding}" Foreground="Red" Background="Transparent" BorderThickness="0"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>