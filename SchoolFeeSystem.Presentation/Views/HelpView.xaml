<UserControl x:Class="SchoolFeeSystem.Presentation.Views.HelpView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="900" Background="White">
    <Grid Margin="40">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">

            <TextBlock Text="Customer Support" FontSize="32" FontWeight="Bold" Foreground="#2C3E50" 
                       HorizontalAlignment="Center" Margin="0,0,0,30"/>

            <Border Background="#F8F9FA" CornerRadius="10" Padding="40" Width="500">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="15" Color="#DDD" ShadowDepth="2"/>
                </Border.Effect>

                <StackPanel>
                    <TextBlock Text="Need Help?" FontSize="22" FontWeight="SemiBold" Margin="0,0,0,20" HorizontalAlignment="Center"/>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                        <TextBlock Text="📧" FontSize="24" VerticalAlignment="Center" Margin="0,0,15,0"/>
                        <StackPanel>
                            <TextBlock Text="Email Us" Foreground="Gray" FontSize="12"/>
                            <TextBlock Text="{Binding SupportEmail}" FontSize="18" FontWeight="SemiBold"/>
                        </StackPanel>
                        <Button Content="Send Mail" Command="{Binding SendEmailCommand}" 
                                Margin="20,0,0,0" Padding="15,5" Background="#3498DB" Foreground="White" BorderThickness="0" VerticalAlignment="Center">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="5"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                    </StackPanel>

                    <Separator Margin="0,0,0,20"/>

                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📞" FontSize="24" VerticalAlignment="Center" Margin="0,0,15,0"/>
                        <StackPanel>
                            <TextBlock Text="Call Us" Foreground="Gray" FontSize="12"/>
                            <TextBlock Text="{Binding SupportPhone}" FontSize="18" FontWeight="SemiBold"/>
                        </StackPanel>
                        <Button Content="Copy Number" Command="{Binding CopyNumberCommand}" 
                                Margin="20,0,0,0" Padding="15,5" Background="#2ECC71" Foreground="White" BorderThickness="0" VerticalAlignment="Center">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="5"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                    </StackPanel>

                </StackPanel>
            </Border>

            <TextBlock Text="Available Mon-Fri, 9AM - 5PM" Foreground="Gray" HorizontalAlignment="Center" Margin="0,20,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>