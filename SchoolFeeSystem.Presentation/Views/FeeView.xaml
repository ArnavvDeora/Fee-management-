<UserControl x:Class="SchoolFeeSystem.Presentation.Views.FeeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="800" Background="White">
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Margin="0,0,20,0">
            <TextBlock Text="Create Fee Structure" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

            <TextBlock Text="Select Class"/>
            <ComboBox ItemsSource="{Binding Classes}" SelectedItem="{Binding SelectedClass}" 
                      DisplayMemberPath="DisplayName" Margin="0,0,0,15" Padding="5"/>

            <TextBlock Text="Fee Name (e.g. Tuition)"/>
            <TextBox Text="{Binding FeeName}" Margin="0,0,0,15" Padding="5"/>

            <TextBlock Text="Amount"/>
            <TextBox Text="{Binding Amount}" Margin="0,0,0,15" Padding="5"/>

            <TextBlock Text="Due Date"/>
            <DatePicker SelectedDate="{Binding DueDate}" Margin="0,0,0,20"/>

            <Button Content="Save Fee" Command="{Binding SaveFeeCommand}" Height="40" Background="#3498DB" Foreground="White"/>
        </StackPanel>

        <DockPanel Grid.Column="1">
            <TextBlock Text="Existing Fees for Selected Class" DockPanel.Dock="Top" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
            <DataGrid ItemsSource="{Binding ClassFees}" AutoGenerateColumns="False" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Fee Name" Binding="{Binding FeeName}" Width="*"/>
                    <DataGridTextColumn Header="Amount" Binding="{Binding Amount, StringFormat=C}" Width="100"/>
                    <DataGridTextColumn Header="Due Date" Binding="{Binding DueDate, StringFormat=d}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>
        </DockPanel>
    </Grid>
</UserControl>