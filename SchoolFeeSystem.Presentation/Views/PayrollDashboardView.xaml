<UserControl x:Class="SchoolFeeSystem.Presentation.Views.PayrollDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="800" d:DesignWidth="1200">

    <UserControl.Resources>
        <Style x:Key="DashboardCardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="0,0,20,0"/>
            <Setter Property="BorderBrush" Value="#EAEAEA"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#F0F0F0" BlurRadius="15" ShadowDepth="3" Direction="270" Opacity="0.5"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SectionContainerStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="BorderBrush" Value="#EAEAEA"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style x:Key="StatusBadgeStyle" TargetType="TextBlock">
            <Setter Property="Padding" Value="10,4"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Status}" Value="Paid">
                    <Setter Property="Background" Value="#D4EDDA"/>
                    <Setter Property="Foreground" Value="#155724"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Status}" Value="Pending">
                    <Setter Property="Background" Value="#FFF3CD"/>
                    <Setter Property="Foreground" Value="#856404"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="240"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="#2C3E50">
            <StackPanel Margin="15">
                <TextBlock Text="HR &amp; PAYROLL" Foreground="White" FontSize="18" FontWeight="Bold" 
                           HorizontalAlignment="Left" Margin="10,25,0,40"/>

                <Border Background="#34495E" CornerRadius="8" Margin="0,5">
                    <Button Content="Dashboard" FontWeight="Bold" Height="45" Foreground="White" BorderThickness="0" Background="Transparent" HorizontalContentAlignment="Left" FontSize="14" Padding="15,0"/>
                </Border>

                <Button Content="Manage Staff" Command="{Binding GoToManageStaffCommand}" Height="45" Margin="0,5" Background="Transparent" Foreground="#BDC3C7" BorderThickness="0" HorizontalContentAlignment="Left" FontSize="14" Padding="15,0"/>
                <Button Content="Salary Setup" Height="45" Margin="0,5" Background="Transparent" Foreground="#BDC3C7" BorderThickness="0" HorizontalContentAlignment="Left" FontSize="14" Padding="15,0"/>
                <Button Content="Process Payroll" Height="45" Margin="0,5" Background="Transparent" Foreground="#BDC3C7" BorderThickness="0" HorizontalContentAlignment="Left" FontSize="14" Padding="15,0"/>
                <Button Content="Salary Reports" Height="45" Margin="0,5" Background="Transparent" Foreground="#BDC3C7" BorderThickness="0" HorizontalContentAlignment="Left" FontSize="14" Padding="15,0"/>

                <Button Content="⬅ Back to Hub" Command="{Binding GoBackCommand}" 
                        Height="45" Margin="0,60,0,5" Background="#E74C3C" Foreground="White" 
                        BorderThickness="0" FontWeight="Bold">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                </Button>

                <Button Content="Logout" Command="{Binding LogoutCommand}" 
                        Height="45" Margin="0,5" Background="#7F8C8D" Foreground="White" 
                        BorderThickness="0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Border>
        <ScrollViewer Grid.Column="1" Background="#F8F9FA" VerticalScrollBarVisibility="Auto">
            <Grid Margin="30">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Payroll Dashboard" FontSize="26" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,25"/>

                <Grid Grid.Row="1" Margin="0,0,0,30">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Style="{StaticResource DashboardCardStyle}">
                        <StackPanel>
                            <Grid>
                                <TextBlock Text="👥" FontSize="26" HorizontalAlignment="Left" VerticalAlignment="Center" Opacity="0.7"/>
                                <TextBlock Text="{Binding TotalEmployees}" FontSize="32" FontWeight="Bold" Foreground="#2C3E50" HorizontalAlignment="Right"/>
                            </Grid>
                            <TextBlock Text="Total Employees" Foreground="#7F8C8D" FontSize="14" Margin="0,10,0,0" FontWeight="SemiBold"/>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="1" Style="{StaticResource DashboardCardStyle}">
                        <StackPanel>
                            <Grid>
                                <TextBlock Text="💰" FontSize="26" HorizontalAlignment="Left" VerticalAlignment="Center" Opacity="0.7"/>
                                <TextBlock Text="{Binding TotalMonthlyPayout, StringFormat={}{0:C0}}" FontSize="32" FontWeight="Bold" Foreground="#27AE60" HorizontalAlignment="Right"/>
                            </Grid>
                            <TextBlock Text="Total Monthly Payout" Foreground="#7F8C8D" FontSize="14" Margin="0,10,0,0" FontWeight="SemiBold"/>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="2" Style="{StaticResource DashboardCardStyle}">
                        <StackPanel>
                            <Grid>
                                <TextBlock Text="⏳" FontSize="26" HorizontalAlignment="Left" VerticalAlignment="Center" Opacity="0.7"/>
                                <TextBlock Text="{Binding PendingSalariesCount}" FontSize="32" FontWeight="Bold" Foreground="#F39C12" HorizontalAlignment="Right"/>
                            </Grid>
                            <TextBlock Text="Pending Salaries" Foreground="#7F8C8D" FontSize="14" Margin="0,10,0,0" FontWeight="SemiBold"/>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="3" Style="{StaticResource DashboardCardStyle}" Margin="0">
                        <StackPanel>
                            <Grid>
                                <TextBlock Text="✅" FontSize="26" HorizontalAlignment="Left" VerticalAlignment="Center" Opacity="0.7"/>
                                <TextBlock Text="{Binding PaidSalariesCount}" FontSize="32" FontWeight="Bold" Foreground="#27AE60" HorizontalAlignment="Right"/>
                            </Grid>
                            <TextBlock Text="Paid Salaries" Foreground="#7F8C8D" FontSize="14" Margin="0,10,0,0" FontWeight="SemiBold"/>
                        </StackPanel>
                    </Border>
                </Grid>

                <Grid Grid.Row="2" Margin="0,0,0,30">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Style="{StaticResource SectionContainerStyle}" Margin="0,0,20,0" Height="300">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Text="Salary Payout Trend (Last 4 Months)" FontSize="16" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,20"/>

                            <ItemsControl Grid.Row="1" ItemsSource="{Binding SalaryTrend}" VerticalAlignment="Bottom">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <UniformGrid Rows="1"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="15,0">
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="*"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>
                                            <Border Grid.Row="0" Background="#3498DB" CornerRadius="6,6,0,0" ToolTip="{Binding FormattedLabel}">
                                                <Border.LayoutTransform>
                                                    <ScaleTransform ScaleY="{Binding ValueScale}"/>
                                                </Border.LayoutTransform>
                                            </Border>
                                            <TextBlock Grid.Row="1" Text="{Binding FormattedLabel}" FontSize="11" Foreground="#7F8C8D" HorizontalAlignment="Center" Margin="0,8,0,2"/>
                                            <TextBlock Grid.Row="2" Text="{Binding Month}" FontWeight="Bold" Foreground="#2C3E50" HorizontalAlignment="Center"/>
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Grid>
                    </Border>

                    <Border Grid.Column="1" Style="{StaticResource SectionContainerStyle}" Height="300">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Text="Recent Activity" FontSize="16" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,20"/>

                            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                                <ItemsControl ItemsSource="{Binding RecentActivities}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                                <Border Width="8" Height="8" CornerRadius="4" Background="#3498DB" Margin="0,6,10,0" VerticalAlignment="Top"/>
                                                <TextBlock Text="{Binding}" TextWrapping="Wrap" Foreground="#34495E" FontSize="13"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                        </Grid>
                    </Border>
                </Grid>

                <Border Grid.Row="3" Style="{StaticResource SectionContainerStyle}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Grid Grid.Row="0" Margin="0,0,0,20">
                            <TextBlock Text="Recent Pay Slips" FontSize="16" FontWeight="Bold" Foreground="#2C3E50" VerticalAlignment="Center"/>
                            <Button Content="View All History" HorizontalAlignment="Right" Background="Transparent" Foreground="#3498DB" BorderThickness="0" Cursor="Hand" FontWeight="SemiBold"/>
                        </Grid>

                        <DataGrid Grid.Row="1" ItemsSource="{Binding RecentPaySlips}" AutoGenerateColumns="False" 
                                  CanUserAddRows="False" IsReadOnly="True" HeadersVisibility="Column"
                                  BorderThickness="0" Background="Transparent" RowBackground="White" AlternatingRowBackground="#F8F9FA"
                                  GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#EEEEEE" Padding="0">

                            <DataGrid.Resources>
                                <Style TargetType="DataGridColumnHeader">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="Foreground" Value="#7F8C8D"/>
                                    <Setter Property="Padding" Value="15,10"/>
                                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                    <Setter Property="BorderBrush" Value="#E0E0E0"/>
                                </Style>
                                <Style TargetType="DataGridCell">
                                    <Setter Property="Padding" Value="15,12"/>
                                    <Setter Property="BorderThickness" Value="0"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="DataGridCell">
                                                <Border Padding="{TemplateBinding Padding}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" SnapsToDevicePixels="True">
                                                    <ContentPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </DataGrid.Resources>

                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Emp ID" Binding="{Binding EmpId}" Width="Auto" Foreground="#34495E"/>

                                <DataGridTemplateColumn Header="Employee Name" Width="*">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal">
                                                <Border Width="30" Height="30" CornerRadius="15" Background="#E0E0E0" Margin="0,0,12,0">
                                                    <TextBlock Text="{Binding Name[0]}" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#7F8C8D" FontWeight="Bold"/>
                                                </Border>
                                                <TextBlock Text="{Binding Name}" VerticalAlignment="Center" FontWeight="SemiBold" Foreground="#2C3E50" FontSize="14"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>

                                <DataGridTextColumn Header="Month" Binding="{Binding Month}" Width="Auto" Foreground="#34495E"/>
                                <DataGridTextColumn Header="Amount" Binding="{Binding Amount, StringFormat={}{0:C0}}" Width="Auto" FontWeight="Bold" Foreground="#2C3E50"/>

                                <DataGridTemplateColumn Header="Status" Width="Auto">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Border CornerRadius="12">
                                                <TextBlock Text="{Binding Status}" Style="{StaticResource StatusBadgeStyle}"/>
                                            </Border>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>

                                <DataGridTemplateColumn Width="50">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="..." FontSize="16" FontWeight="Bold" Foreground="#BDC3C7" HorizontalAlignment="Center" Cursor="Hand"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </Border>
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>