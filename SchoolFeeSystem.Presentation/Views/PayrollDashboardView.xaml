<UserControl x:Class="SchoolFeeSystem.Presentation.Views.PayrollDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="700" d:DesignWidth="1100" Background="#F8F9FA">

    <Grid Margin="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,40">
            <TextBlock Text="Payroll Dashboard" FontSize="32" FontWeight="Bold" Foreground="#2C3E50"/>
            <TextBlock Text="Manage staff, attendance, holidays, and salary processing." FontSize="16" Foreground="#7F8C8D" Margin="0,5,0,0"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" BorderThickness="0">
            <WrapPanel Orientation="Horizontal" HorizontalAlignment="Left">

                <!-- Staff Directory -->
                <Button Command="{Binding GoToManageStaffCommand}" Width="220" Height="140" Margin="0,0,20,20" Background="White" BorderThickness="0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="12"/>
                            <Setter Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect BlurRadius="15" Color="#E0E0E0" Opacity="0.6" ShadowDepth="3"/>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Resources>
                    <StackPanel>
                        <TextBlock Text="👥" FontSize="36" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                        <TextBlock Text="Staff Directory" FontWeight="Bold" FontSize="16" Foreground="#2C3E50" HorizontalAlignment="Center"/>
                        <TextBlock Text="Add, Edit, View Staff" FontSize="12" Foreground="#95A5A6" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                    </StackPanel>
                </Button>

                <!-- Salary Setup 
                <Button Command="{Binding GoToSalarySetupCommand}" Width="220" Height="140" Margin="0,0,20,20" Background="White" BorderThickness="0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="12"/>
                            <Setter Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect BlurRadius="15" Color="#E0E0E0" Opacity="0.6" ShadowDepth="3"/>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Resources>
                    <StackPanel>
                        <TextBlock Text="⚙️" FontSize="36" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                        <TextBlock Text="Salary Setup" FontWeight="Bold" FontSize="16" Foreground="#2C3E50" HorizontalAlignment="Center"/>
                        <TextBlock Text="Base Pay &amp; Allowances" FontSize="12" Foreground="#95A5A6" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                    </StackPanel>
                </Button>
                -->
                <!-- Attendance -->
                <Button Command="{Binding GoToAttendanceCommand}" Width="220" Height="140" Margin="0,0,20,20" Background="White" BorderThickness="0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="12"/>
                            <Setter Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect BlurRadius="15" Color="#E0E0E0" Opacity="0.6" ShadowDepth="3"/>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Resources>
                    <StackPanel>
                        <TextBlock Text="📅" FontSize="36" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                        <TextBlock Text="Attendance" FontWeight="Bold" FontSize="16" Foreground="#2C3E50" HorizontalAlignment="Center"/>
                        <TextBlock Text="Biometric Import &amp; Edit" FontSize="12" Foreground="#95A5A6" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                    </StackPanel>
                </Button>

                <!-- Holidays -->
                <Button Command="{Binding GoToHolidaysCommand}" Width="220" Height="140" Margin="0,0,20,20" Background="White" BorderThickness="0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="12"/>
                            <Setter Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect BlurRadius="15" Color="#E0E0E0" Opacity="0.6" ShadowDepth="3"/>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Resources>
                    <StackPanel>
                        <TextBlock Text="🎉" FontSize="36" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                        <TextBlock Text="Holidays" FontWeight="Bold" FontSize="16" Foreground="#2C3E50" HorizontalAlignment="Center"/>
                        <TextBlock Text="Set Yearly Holidays" FontSize="12" Foreground="#95A5A6" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                    </StackPanel>
                </Button>

                <!-- Allowance Time (NEW - Matching Style) -->
                <Button Command="{Binding GoToAllowanceTimeCommand}" Width="220" Height="140" Margin="0,0,20,20" Background="White" BorderThickness="0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="12"/>
                            <Setter Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect BlurRadius="15" Color="#E0E0E0" Opacity="0.6" ShadowDepth="3"/>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Resources>
                    <StackPanel>
                        <TextBlock Text="⏱️" FontSize="36" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                        <TextBlock Text="Allowance Time" FontWeight="Bold" FontSize="16" Foreground="#2C3E50" HorizontalAlignment="Center"/>
                        <TextBlock Text="View OT Time Bank" FontSize="12" Foreground="#95A5A6" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                    </StackPanel>
                </Button>
                <!-- Leave Management Card -->
                <Button Command="{Binding GoToLeaveManagementCommand}" Width="220" Height="140" Margin="0,0,20,20" Background="White" BorderThickness="0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="12"/>
                            <Setter Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect BlurRadius="15" Color="#E0E0E0" Opacity="0.6" ShadowDepth="3"/>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Resources>
                    <StackPanel>
                        <TextBlock Text="📋" FontSize="36" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                        <TextBlock Text="Leave Management" FontWeight="Bold" FontSize="16" Foreground="#2C3E50" HorizontalAlignment="Center"/>
                        <TextBlock Text="Grant &amp; Track Leaves" FontSize="12" Foreground="#95A5A6" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                    </StackPanel>
                </Button>

                <!-- Process Payroll (FIXED - Changed to White Background) -->
                <Button Command="{Binding GoToProcessPayrollCommand}" Width="220" Height="140" Margin="0,0,20,20" Background="White" BorderThickness="0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="12"/>
                            <Setter Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect BlurRadius="15" Color="#E0E0E0" Opacity="0.6" ShadowDepth="3"/>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Resources>
                    <StackPanel>
                        <TextBlock Text="💸" FontSize="36" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                        <TextBlock Text="Process Payroll" FontWeight="Bold" FontSize="16" Foreground="#2C3E50" HorizontalAlignment="Center"/>
                        <TextBlock Text="Calculate &amp; Finalize" FontSize="12" Foreground="#95A5A6" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                    </StackPanel>
                </Button>

                <!-- Reports Center 
                <Button Command="{Binding GoToPayrollReportsCommand}" Width="220" Height="140" Margin="0,0,20,20" Background="White" BorderThickness="0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="12"/>
                            <Setter Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect BlurRadius="15" Color="#E0E0E0" Opacity="0.6" ShadowDepth="3"/>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Resources>
                    <StackPanel>
                        <TextBlock Text="📊" FontSize="36" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                        <TextBlock Text="Reports Center" FontWeight="Bold" FontSize="16" Foreground="#2C3E50" HorizontalAlignment="Center"/>
                        <TextBlock Text="Attendance &amp; Salary Logs" FontSize="12" Foreground="#95A5A6" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                    </StackPanel>
                </Button>
                -->

            </WrapPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
