<UserControl x:Class="SchoolFeeSystem.Presentation.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="900" Background="#F5F7FA">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#27AE60" CornerRadius="8" Padding="20" Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock Text="Today's Collection" Foreground="White" FontSize="16"/>
                    <TextBlock Text="{Binding TodayCollection, StringFormat=C}" Foreground="White" FontSize="28" FontWeight="Bold" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Background="#E74C3C" CornerRadius="8" Padding="20" Margin="5,0,5,0">
                <StackPanel>
                    <TextBlock Text="Total Pending Dues" Foreground="White" FontSize="16"/>
                    <TextBlock Text="{Binding TotalPending, StringFormat=C}" Foreground="White" FontSize="28" FontWeight="Bold" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Background="#3498DB" CornerRadius="8" Padding="20" Margin="10,0,0,0">
                <StackPanel>
                    <TextBlock Text="Total Active Students" Foreground="White" FontSize="16"/>
                    <TextBlock Text="{Binding StudentCount}" Foreground="White" FontSize="28" FontWeight="Bold" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>
        </Grid>

        <DockPanel Grid.Row="1" Background="White">
            <Grid DockPanel.Dock="Top" Margin="10">
                <TextBlock Text="Recent Transactions" FontSize="18" FontWeight="Bold" VerticalAlignment="Center"/>
                <Button Content="Refresh Data" Command="{Binding RefreshCommand}" HorizontalAlignment="Right" Padding="10,5"/>
            </Grid>

            <DataGrid ItemsSource="{Binding RecentTransactions}" AutoGenerateColumns="False" IsReadOnly="True" BorderThickness="0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Date" Binding="{Binding PaymentDate, StringFormat=g}" Width="150"/>
                    <DataGridTextColumn Header="Student Name" Binding="{Binding StudentFee.Student.FullName}" Width="*"/>
                    <DataGridTextColumn Header="Amount" Binding="{Binding AmountPaid, StringFormat=C}" Width="100" FontWeight="Bold"/>
                    <DataGridTextColumn Header="Mode" Binding="{Binding PaymentMode}" Width="80"/>
                </DataGrid.Columns>
            </DataGrid>
        </DockPanel>
    </Grid>
</UserControl>