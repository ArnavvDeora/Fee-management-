<UserControl x:Class="SchoolFeeSystem.Presentation.Views.StudentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800" Background="White">
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto" Margin="0,0,20,0">
            <StackPanel>
                <TextBlock Text="Add New Student" FontSize="22" FontWeight="Bold" Margin="0,0,0,20" Foreground="#2C3E50"/>

                <TextBlock Text="Roll Number" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <TextBox Text="{Binding NewStudent.RollNumber}" Padding="5" Margin="0,0,0,5"/>

                <TextBlock Text="Full Name" FontWeight="SemiBold" Margin="0,10,0,5"/>
                <TextBox Text="{Binding NewStudent.FullName}" Padding="5" Margin="0,0,0,5"/>

                <TextBlock Text="Father Name" FontWeight="SemiBold" Margin="0,10,0,5"/>
                <TextBox Text="{Binding NewStudent.FatherName}" Padding="5" Margin="0,0,0,5"/>

                <TextBlock Text="Father's Contact" FontWeight="SemiBold" Margin="0,10,0,5"/>
                <TextBox Text="{Binding NewStudent.FatherContact}" Padding="5" Margin="0,0,0,5"/>

                <TextBlock Text="Student Contact" FontWeight="SemiBold" Margin="0,10,0,5"/>
                <TextBox Text="{Binding NewStudent.ContactNumber}" Padding="5" Margin="0,0,0,5"/>

                <TextBlock Text="Address" FontWeight="SemiBold" Margin="0,10,0,5"/>
                <TextBox Text="{Binding NewStudent.Address}" Padding="5" Margin="0,0,0,5" Height="60" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>

                <TextBlock Text="Select Standard" FontWeight="SemiBold" Margin="0,10,0,5"/>
                <ComboBox ItemsSource="{Binding Standards}" SelectedItem="{Binding SelectedStandard}" 
                          Padding="5" Margin="0,0,0,5"/>

                <TextBlock Text="Select Section" FontWeight="SemiBold" Margin="0,10,0,5"/>
                <ComboBox ItemsSource="{Binding Sections}" SelectedItem="{Binding SelectedSection}" 
                          Padding="5" Margin="0,0,0,20"/>

                <Button Content="Save Student" Command="{Binding AddStudentCommand}" 
                        Height="45" FontSize="16" FontWeight="Bold" Margin="0,0,0,20"
                        Background="#2ECC71" Foreground="White" BorderThickness="0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </ScrollViewer>
        <DockPanel Grid.Column="1">
            <TextBlock Text="Student List" DockPanel.Dock="Top" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>
            <DataGrid ItemsSource="{Binding Students}" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                    <DataGridTextColumn Header="Roll No" Binding="{Binding RollNumber}" Width="60"/>
                    <DataGridTextColumn Header="Name" Binding="{Binding FullName}" Width="*"/>
                    <DataGridTextColumn Header="Father" Binding="{Binding FatherName}" Width="150"/>
                    <DataGridTextColumn Header="Class" Binding="{Binding Class.DisplayName}" Width="80"/>
                    <DataGridTextColumn Header="Contact" Binding="{Binding ContactNumber}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>
        </DockPanel>
    </Grid>
</UserControl>