<UserControl x:Class="SchoolFeeSystem.Presentation.Views.StudentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800" Background="White">
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Margin="0,0,20,0">
            <TextBlock Text="Add New Student" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

            <TextBlock Text="Full Name"/>
            <TextBox Text="{Binding NewStudent.FullName}" Margin="0,0,0,10" Padding="5"/>

            <TextBlock Text="Father Name"/>
            <TextBox Text="{Binding NewStudent.FatherName}" Margin="0,0,0,10" Padding="5"/>

            <TextBlock Text="Contact Number"/>
            <TextBox Text="{Binding NewStudent.ContactNumber}" Margin="0,0,0,10" Padding="5"/>

            <TextBlock Text="Select Standard" FontWeight="SemiBold" Margin="0,10,0,5"/>
            <ComboBox ItemsSource="{Binding Standards}" SelectedItem="{Binding SelectedStandard}" 
                      Padding="5" Margin="0,0,0,5"/>

            <TextBlock Text="Select Section" FontWeight="SemiBold" Margin="0,10,0,5"/>
            <ComboBox ItemsSource="{Binding Sections}" SelectedItem="{Binding SelectedSection}" 
                      Padding="5" Margin="0,0,0,20"/>
            <Button Content="Save Student" Command="{Binding AddStudentCommand}" Height="40" Background="#2ECC71" Foreground="White" FontWeight="Bold"/>
        </StackPanel>

        <DockPanel Grid.Column="1">
            <TextBlock Text="Student List" DockPanel.Dock="Top" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>
            <DataGrid ItemsSource="{Binding Students}" AutoGenerateColumns="False" IsReadOnly="True" GridLinesVisibility="Horizontal">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                    <DataGridTextColumn Header="Name" Binding="{Binding FullName}" Width="*"/>
                    <DataGridTextColumn Header="Father" Binding="{Binding FatherName}" Width="150"/>
                    <DataGridTextColumn Header="Class" Binding="{Binding Class.DisplayName}" Width="80"/>
                    <DataGridTextColumn Header="Contact" Binding="{Binding ContactNumber}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>
        </DockPanel>
    </Grid>
</UserControl>