<UserControl x:Class="SchoolFeeSystem.Presentation.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="550" d:DesignWidth="450">

    <Border Background="White" CornerRadius="10" Padding="20">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="0" Color="#BDC3C7"/>
        </Border.Effect>
        <Grid>
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="Welcome Back" FontSize="28" FontWeight="Bold" Foreground="#2C3E50" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                <TextBlock Text="Login to continue" FontSize="14" Foreground="#7F8C8D" HorizontalAlignment="Center" Margin="0,0,0,30"/>

                <TextBlock Text="Username" FontWeight="SemiBold" Foreground="#34495E" Margin="0,0,0,5"/>
                <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Padding="10" Margin="0,0,0,15"/>

                <TextBlock Text="Password" FontWeight="SemiBold" Foreground="#34495E" Margin="0,0,0,5"/>
                <PasswordBox x:Name="txtPassword" Padding="10" Margin="0,0,0,20"/>

                <Button Content="Login" Command="{Binding LoginCommand}" CommandParameter="{Binding ElementName=txtPassword}"
                        Height="45" Background="#2980B9" Foreground="White" FontSize="16" FontWeight="Bold" BorderThickness="0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>