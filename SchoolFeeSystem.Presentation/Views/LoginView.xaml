<Window x:Class="SchoolFeeSystem.Presentation.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="School Fee System - Login" Height="450" Width="800"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid Background="#F0F2F5">
        <Border Background="White" Width="350" Height="400" CornerRadius="10">
            <StackPanel VerticalAlignment="Center" Margin="30">
                <TextBlock Text="Welcome Back" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,30"/>

                <TextBlock Text="Username" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <TextBox Text="{Binding Username}" Padding="5" Height="30" Margin="0,0,0,15"/>

                <TextBlock Text="Password" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <PasswordBox x:Name="txtPassword" Padding="5" Height="30" Margin="0,0,0,20"/>

                <Button Content="LOGIN" Height="40" Background="#007ACC" Foreground="White" 
                        FontSize="14" FontWeight="Bold" Click="Button_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>