<UserControl x:Class="SchoolFeeSystem.Presentation.Views.AllowanceTimeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">

    <Grid Background="#F5F5F5" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
            <Button Content="← Back" 
                    Command="{Binding GoBackCommand}"
                    Background="#2196F3" 
                    Foreground="White"
                    Padding="15,8"
                    Margin="0,0,20,0"
                    FontSize="14"
                    Cursor="Hand"/>
            <TextBlock Text="Overtime Allowance Time Bank" 
                       FontSize="24" 
                       FontWeight="Bold" 
                       Foreground="#333"
                       VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Filters & Search -->
        <Border Grid.Row="1" 
                Background="White" 
                BorderBrush="#DDD" 
                BorderThickness="1" 
                CornerRadius="5"
                Padding="15"
                Margin="0,0,0,15">
            <StackPanel>
                <TextBlock Text="Search &amp; Filter" 
                           FontSize="16" 
                           FontWeight="SemiBold" 
                           Foreground="#555"
                           Margin="0,0,0,10"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Search Box -->
                    <TextBox Grid.Column="0"
                             Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}"
                             FontSize="14"
                             Padding="10"
                             BorderBrush="#CCC"
                             Margin="0,0,10,0">
                        <TextBox.Style>
                            <Style TargetType="TextBox">
                                <Setter Property="Foreground" Value="#333"/>
                                <Style.Triggers>
                                    <Trigger Property="Text" Value="">
                                        <Setter Property="Background">
                                            <Setter.Value>
                                                <VisualBrush AlignmentX="Left" AlignmentY="Center" Stretch="None">
                                                    <VisualBrush.Visual>
                                                        <TextBlock Text="🔍 Search by name, department, or Bio ID..." 
                                                                   Foreground="#999" 
                                                                   Margin="10,0,0,0"/>
                                                    </VisualBrush.Visual>
                                                </VisualBrush>
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </TextBox.Style>
                    </TextBox>

                    <!-- Search Button -->
                    <Button Grid.Column="1"
                            Content="Search"
                            Command="{Binding SearchFilterCommand}"
                            Background="#4CAF50"
                            Foreground="White"
                            Padding="20,10"
                            Margin="0,0,10,0"
                            FontSize="14"
                            Cursor="Hand"/>

                    <!-- Refresh Button -->
                    <Button Grid.Column="2"
                            Content="↻ Refresh"
                            Command="{Binding LoadDataCommand}"
                            Background="#FF9800"
                            Foreground="White"
                            Padding="20,10"
                            FontSize="14"
                            Cursor="Hand"/>
                </Grid>
            </StackPanel>
        </Border>

        <!-- Data Grid -->
        <Border Grid.Row="2" 
                Background="White" 
                BorderBrush="#DDD" 
                BorderThickness="1" 
                CornerRadius="5">
            <DataGrid ItemsSource="{Binding AllowanceList}" 
                      SelectedItem="{Binding SelectedItem}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      RowHeight="35"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      AlternatingRowBackground="#F9F9F9"
                      BorderThickness="0">

                <DataGrid.Resources>
                    <!-- Header Style -->
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#2196F3"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="FontSize" Value="14"/>
                        <Setter Property="Padding" Value="10,8"/>
                        <Setter Property="BorderBrush" Value="#1976D2"/>
                        <Setter Property="BorderThickness" Value="0,0,1,0"/>
                    </Style>

                    <!-- Cell Style -->
                    <Style TargetType="DataGridCell">
                        <Setter Property="Padding" Value="10,5"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                    </Style>
                </DataGrid.Resources>

                <DataGrid.Columns>
                    <!-- Employee Name -->
                    <DataGridTextColumn Header="Employee Name" 
                                        Binding="{Binding EmployeeName}" 
                                        Width="200">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="Foreground" Value="#333"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <!-- Department -->
                    <DataGridTextColumn Header="Department" 
                                        Binding="{Binding Department}" 
                                        Width="150">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="#666"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <!-- Bio ID -->
                    <DataGridTextColumn Header="Bio ID" 
                                        Binding="{Binding BiometricId}" 
                                        Width="100">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="#999"/>
                                <Setter Property="FontFamily" Value="Consolas"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <!-- Total Hours -->
                    <DataGridTextColumn Header="Total Earned (hrs)" 
                                        Binding="{Binding TotalHours, StringFormat=F2}" 
                                        Width="130">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="#4CAF50"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="TextAlignment" Value="Right"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <!-- Used Hours -->
                    <DataGridTextColumn Header="Used (hrs)" 
                                        Binding="{Binding UsedHours, StringFormat=F2}" 
                                        Width="110">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="#FF5722"/>
                                <Setter Property="TextAlignment" Value="Right"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <!-- Available Hours -->
                    <DataGridTextColumn Header="Available (hrs)" 
                                        Binding="{Binding AvailableHours, StringFormat=F2}" 
                                        Width="130">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="TextAlignment" Value="Right"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding StatusColor}" Value="Green">
                                        <Setter Property="Foreground" Value="#4CAF50"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding StatusColor}" Value="Orange">
                                        <Setter Property="Foreground" Value="#FF9800"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding StatusColor}" Value="Red">
                                        <Setter Property="Foreground" Value="#F44336"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <!-- Last Updated -->
                    <DataGridTextColumn Header="Last Updated" 
                                        Binding="{Binding LastUpdated}" 
                                        Width="150">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="#999"/>
                                <Setter Property="FontSize" Value="12"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- Footer Actions -->
        <Border Grid.Row="3" 
                Background="White" 
                BorderBrush="#DDD" 
                BorderThickness="1" 
                CornerRadius="5"
                Padding="15"
                Margin="0,15,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBlock Text="{Binding AllowanceList.Count, StringFormat='Total Employees: {0}'}" 
                           FontSize="14"
                           FontWeight="SemiBold"
                           Foreground="#555"
                           VerticalAlignment="Center"
                           Margin="0,0,20,0"/>

                <Button Content="📊 Export to CSV"
                        Command="{Binding ExportToCsvCommand}"
                        Background="#673AB7"
                        Foreground="White"
                        Padding="20,10"
                        FontSize="14"
                        Cursor="Hand"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
