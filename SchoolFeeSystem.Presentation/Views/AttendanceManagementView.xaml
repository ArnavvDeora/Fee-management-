<UserControl x:Class="SchoolFeeSystem.Presentation.Views.AttendanceManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="800" d:DesignWidth="1200" Background="#F8F9FA">

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <Button Command="{Binding GoBackCommand}" Content="⬅ Back to Directory" HorizontalAlignment="Left" Background="Transparent" Foreground="#7F8C8D" BorderThickness="0" FontWeight="Bold" Margin="0,0,0,10"/>
            <TextBlock Text="Attendance Management" FontSize="28" FontWeight="Bold" Foreground="#2C3E50"/>
            <TextBlock Text="Import biometric logs, view records, and manage leaves." Foreground="#7F8C8D" FontSize="14"/>
        </StackPanel>

        <Border Grid.Row="1" Background="White" CornerRadius="10" Padding="20" Margin="0,0,0,20">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" Color="#E0E0E0" Opacity="0.5"/>
            </Border.Effect>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <StackPanel Margin="0,0,20,0">
                        <TextBlock Text="Select Employee" FontWeight="SemiBold" Foreground="#7F8C8D" FontSize="12"/>
                        <ComboBox ItemsSource="{Binding Employees}" SelectedItem="{Binding SelectedEmployee}" DisplayMemberPath="FullName" Width="200" Padding="10" Margin="0,5,0,0"/>
                    </StackPanel>

                    <StackPanel Margin="0,0,20,0">
                        <TextBlock Text="Select Month" FontWeight="SemiBold" Foreground="#7F8C8D" FontSize="12"/>
                        <DatePicker SelectedDate="{Binding SelectedMonth}" Width="150" Padding="10" Margin="0,5,0,0" VerticalContentAlignment="Center"/>
                    </StackPanel>

                    <Button Content="Filter Data" Command="{Binding LoadAttendanceCommand}" Background="#3498DB" Foreground="White" BorderThickness="0" FontWeight="Bold" Padding="20,0" Height="38" VerticalAlignment="Bottom" Margin="0,0,0,1">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="5"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Command="{Binding ImportExcelCommand}" Background="#27AE60" Foreground="White" BorderThickness="0" Height="45" Padding="20,0">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="6"/>
                            </Style>
                        </Button.Resources>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📥" Margin="0,0,10,0"/>
                            <TextBlock Text="Import Biometric Excel" FontWeight="Bold"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <DataGrid Grid.Row="2" ItemsSource="{Binding AttendanceRecords}" AutoGenerateColumns="False" 
                  CanUserAddRows="False" IsReadOnly="True" HeadersVisibility="Column"
                  BorderThickness="0" Background="White" RowBackground="White" AlternatingRowBackground="#F8F9FA" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#EEEEEE">

            <DataGrid.Resources>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#F1F2F6"/>
                    <Setter Property="Padding" Value="15,12"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="Foreground" Value="#2C3E50"/>
                    <Setter Property="BorderThickness" Value="0"/>
                </Style>
                <Style TargetType="DataGridCell">
                    <Setter Property="Padding" Value="15,10"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="DataGridCell">
                                <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}">
                                    <ContentPresenter VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </DataGrid.Resources>

            <DataGrid.Columns>
                <DataGridTextColumn Header="Date" Binding="{Binding Date, StringFormat=dd-MMM-yyyy}" Width="*" Foreground="#34495E"/>

                <DataGridTemplateColumn Header="Employee" Width="2*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Employee.FullName}" FontWeight="SemiBold" Foreground="#2C3E50"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="In Time" Binding="{Binding InTime}" Width="*" FontWeight="Bold" Foreground="#27AE60"/>
                <DataGridTextColumn Header="Out Time" Binding="{Binding OutTime}" Width="*" FontWeight="Bold" Foreground="#C0392B"/>
                <DataGridTextColumn Header="Duration" Binding="{Binding Duration}" Width="*" Foreground="#7F8C8D"/>

                <DataGridTemplateColumn Header="Status" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border CornerRadius="4" Padding="8,2" HorizontalAlignment="Left">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="#E8F6F3"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Status}" Value="Absent">
                                                <Setter Property="Background" Value="#FDEDEC"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Status}" Value="Sunday">
                                                <Setter Property="Background" Value="#FEF9E7"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock Text="{Binding Status}" FontSize="12" FontWeight="SemiBold">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="#27AE60"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Status}" Value="Absent">
                                                    <Setter Property="Foreground" Value="#C0392B"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Status}" Value="Sunday">
                                                    <Setter Property="Foreground" Value="#F1C40F"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Header="Action" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="✎ Edit" 
                                    Command="{Binding DataContext.EditRecordCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                    CommandParameter="{Binding}"
                                    Background="Transparent" Foreground="#3498DB" BorderThickness="0" FontWeight="Bold" Cursor="Hand"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>