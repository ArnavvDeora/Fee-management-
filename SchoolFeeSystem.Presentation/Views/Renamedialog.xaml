<Window x:Class="SchoolFeeSystem.Presentation.Views.RenameDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit"
        Width="440"
        WindowStartupLocation="CenterOwner"
        SizeToContent="Height" MinHeight="200" ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="#FAFAFA">

    <Grid Margin="24,20,24,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="24"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Label -->
        <TextBlock x:Name="LabelText"
                   Grid.Row="0"
                   Text="Enter new name:"
                   FontSize="13"
                   FontWeight="SemiBold"
                   Foreground="#424242"/>

        <!-- Input -->
        <Border Grid.Row="2"
                BorderBrush="#1976D2"
                BorderThickness="2"
                CornerRadius="4"
                Background="White">
            <TextBox x:Name="NameBox"
                     FontSize="14"
                     Padding="10,7"
                     BorderThickness="0"
                     Background="Transparent"
                     KeyDown="NameBox_KeyDown"/>
        </Border>

        <!-- Buttons -->
        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">

            <Button x:Name="CancelBtn"
                    Content="Cancel"
                    Width="88" Height="34"
                    Margin="0,0,10,0"
                    FontSize="13"
                    Background="#EEEEEE"
                    Foreground="#424242"
                    BorderThickness="1"
                    BorderBrush="#BDBDBD"
                    Cursor="Hand"
                    Click="Cancel_Click">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E0E0E0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>

            <Button x:Name="OkBtn"
                    Content="✔ Save"
                    Width="88" Height="34"
                    FontSize="13"
                    FontWeight="SemiBold"
                    Background="#1976D2"
                    Foreground="White"
                    BorderThickness="0"
                    Cursor="Hand"
                    Click="Ok_Click">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1565C0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>

        </StackPanel>
    </Grid>
</Window>
