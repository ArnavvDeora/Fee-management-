<UserControl x:Class="SchoolFeeSystem.Presentation.Views.PayslipViewerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="800" 
             Background="White">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#F0F0F0" Padding="10">
            <Button Content="🖨 Print / Save as PDF" 
                    Command="{Binding PrintSlipCommand}" 
                    CommandParameter="{Binding ElementName=PrintArea}"
                    HorizontalAlignment="Right" Background="#2C3E50" Foreground="White" Padding="15,5" FontWeight="Bold">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="4"/>
                    </Style>
                </Button.Resources>
            </Button>
        </Border>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Background="#555" Padding="20">
            <Border x:Name="PrintArea" Background="White" Padding="40" HorizontalAlignment="Center" VerticalAlignment="Top" Width="750">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.3"/>
                </Border.Effect>

                <StackPanel>
                    <TextBlock Text="INSTITUTE NAME / HEADER" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Foreground="#2C3E50"/>
                    <TextBlock Text="Salary Slip &amp; Cost Ledger" FontSize="16" HorizontalAlignment="Center" Foreground="Gray" Margin="0,5,0,20"/>

                    <Separator Background="Black" Margin="0,0,0,20"/>

                    <Grid Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <StackPanel Orientation="Horizontal" Margin="0,2">
                                <TextBlock Text="Name: " FontWeight="Bold" Width="100"/>
                                <TextBlock Text="{Binding CurrentSlip.Employee.FullName}"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,2">
                                <TextBlock Text="Designation: " FontWeight="Bold" Width="100"/>
                                <TextBlock Text="{Binding CurrentSlip.Designation}"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,2">
                                <TextBlock Text="Employee ID: " FontWeight="Bold" Width="100"/>
                                <TextBlock Text="{Binding CurrentSlip.Employee.BiometricId}"/>
                            </StackPanel>
                        </StackPanel>

                        <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                            <StackPanel Orientation="Horizontal" Margin="0,2">
                                <TextBlock Text="Month Days: " FontWeight="Bold" Width="120"/>
                                <TextBlock Text="{Binding CurrentSlip.TotalMonthDays}"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,2">
                                <TextBlock Text="Days Worked: " FontWeight="Bold" Width="120"/>
                                <TextBlock Text="{Binding CurrentSlip.DaysWorked}"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,2">
                                <TextBlock Text="Recovery Days: " FontWeight="Bold" Foreground="Red" Width="120"/>
                                <TextBlock Text="{Binding CurrentSlip.RecoveryDays}" Foreground="Red"/>
                            </StackPanel>
                            <Border Background="#EAF2F8" Padding="5,2" Margin="0,5,0,0" HorizontalAlignment="Left" CornerRadius="4">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="PAYABLE DAYS: " FontWeight="Bold" Foreground="#2980B9"/>
                                    <TextBlock Text="{Binding CurrentSlip.PayableDays}" FontWeight="Bold" Foreground="#2980B9"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </Grid>

                    <Border BorderBrush="#2C3E50" BorderThickness="2" Margin="0,0,0,20">
                        <StackPanel>
                            <TextBlock Text="EMPLOYEE SALARY DETAILS" FontWeight="Bold" Background="#2C3E50" Foreground="White" Padding="10"/>

                            <Grid Margin="15">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="1"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" Margin="10,0">
                                    <TextBlock Text="Section A (Regular)" FontWeight="Bold" TextDecorations="Underline" Margin="0,0,0,5"/>
                                    <Grid Margin="0,2">
                                        <TextBlock Text="Basic Salary Rate"/>
                                        <TextBlock Text="{Binding CurrentSlip.BasicSalary, StringFormat=N2}" HorizontalAlignment="Right"/>
                                    </Grid>
                                    <Grid Margin="0,2">
                                        <TextBlock Text="Salary Earned"/>
                                        <TextBlock Text="{Binding CurrentSlip.SalaryEarned, StringFormat=N2}" HorizontalAlignment="Right" FontWeight="SemiBold"/>
                                    </Grid>

                                    <TextBlock Text="Section B (Overtime)" FontWeight="Bold" TextDecorations="Underline" Margin="0,15,0,5"/>
                                    <Grid Margin="0,2">
                                        <TextBlock Text="{Binding CurrentSlip.OT_Hours, StringFormat='OT Amount ({0} hrs)'}"/>
                                        <TextBlock Text="{Binding CurrentSlip.OT_Salary, StringFormat=N2}" HorizontalAlignment="Right"/>
                                    </Grid>

                                    <Separator Margin="0,10"/>
                                    <Grid>
                                        <TextBlock Text="GROSS SALARY" FontWeight="Bold"/>
                                        <TextBlock Text="{Binding CurrentSlip.GrossSalary, StringFormat=N2}" HorizontalAlignment="Right" FontWeight="Bold"/>
                                    </Grid>
                                </StackPanel>

                                <Rectangle Grid.Column="1" Fill="#CCC"/>

                                <StackPanel Grid.Column="2" Margin="10,0">
                                    <TextBlock Text="Deductions" FontWeight="Bold" TextDecorations="Underline" Margin="0,0,0,5"/>
                                    <Grid Margin="0,2">
                                        <TextBlock Text="EPF (Employee)"/>
                                        <TextBlock Text="{Binding CurrentSlip.EPF_Employee, StringFormat=N2}" HorizontalAlignment="Right"/>
                                    </Grid>
                                    <Grid Margin="0,2">
                                        <TextBlock Text="ESI (0.75%)"/>
                                        <TextBlock Text="{Binding CurrentSlip.ESI_Employee, StringFormat=N2}" HorizontalAlignment="Right"/>
                                    </Grid>
                                    <Grid Margin="0,2">
                                        <TextBlock Text="TDS (2.0%)"/>
                                        <TextBlock Text="{Binding CurrentSlip.TDS, StringFormat=N2}" HorizontalAlignment="Right"/>
                                    </Grid>

                                    <Separator Margin="0,10"/>
                                    <Grid>
                                        <TextBlock Text="Total Deductions" FontWeight="Bold" Foreground="Red"/>
                                        <TextBlock Text="{Binding CurrentSlip.TotalDeductions, StringFormat=N2}" HorizontalAlignment="Right" FontWeight="Bold" Foreground="Red"/>
                                    </Grid>
                                </StackPanel>
                            </Grid>

                            <Border Background="#E8F8F5" Padding="15" BorderBrush="#2C3E50" BorderThickness="0,1,0,0">
                                <Grid>
                                    <TextBlock Text="NET PAYABLE AMOUNT" FontSize="16" FontWeight="Bold"/>
                                    <TextBlock Text="{Binding CurrentSlip.NetPaid, StringFormat='₹ {0:N2}'}" FontSize="18" FontWeight="Bold" HorizontalAlignment="Right" Foreground="#27AE60"/>
                                </Grid>
                            </Border>
                        </StackPanel>
                    </Border>

                    <Border BorderBrush="#7F8C8D" BorderThickness="1">
                        <StackPanel>
                            <TextBlock Text="INSTITUTE COST LEDGER (Accounting View)" FontWeight="Bold" Background="#F0F0F0" Padding="10" Foreground="#555"/>

                            <Grid Margin="15">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="1.2*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" Margin="0,0,20,0">
                                    <TextBlock Text="Employer Liabilities" FontWeight="Bold" Margin="0,0,0,5"/>
                                    <Grid Margin="0,2">
                                        <TextBlock Text="EPF Employer (13%)"/>
                                        <TextBlock Text="{Binding CurrentSlip.EPF_Employer, StringFormat=N2}" HorizontalAlignment="Right"/>
                                    </Grid>
                                    <Grid Margin="0,2">
                                        <TextBlock Text="ESI Employer (3.25%)"/>
                                        <TextBlock Text="{Binding CurrentSlip.ESI_Employer, StringFormat=N2}" HorizontalAlignment="Right"/>
                                    </Grid>
                                    <Grid Margin="0,2">
                                        <TextBlock Text="Admin Charges"/>
                                        <TextBlock Text="{Binding CurrentSlip.AdminCharges, StringFormat=N2}" HorizontalAlignment="Right"/>
                                    </Grid>
                                </StackPanel>

                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="Total Cost Calculation" FontWeight="Bold" Margin="0,0,0,5"/>
                                    <Grid Margin="0,2">
                                        <TextBlock Text="Cost Before GST"/>
                                        <TextBlock Text="{Binding CurrentSlip.InstituteCostBeforeGST, StringFormat=N2}" HorizontalAlignment="Right"/>
                                    </Grid>
                                    <Grid Margin="0,2">
                                        <TextBlock Text="GST (18%)"/>
                                        <TextBlock Text="{Binding CurrentSlip.GST_Amount, StringFormat=N2}" HorizontalAlignment="Right"/>
                                    </Grid>
                                    <Separator Margin="0,5"/>
                                    <Grid>
                                        <TextBlock Text="TOTAL COST" FontWeight="Bold"/>
                                        <TextBlock Text="{Binding CurrentSlip.TotalInstituteCost, StringFormat=N2}" HorizontalAlignment="Right" FontWeight="Bold"/>
                                    </Grid>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <Grid Margin="0,40,0,0">
                        <TextBlock Text="Employee Signature" HorizontalAlignment="Left" Foreground="Gray"/>
                        <TextBlock Text="Authorized Signatory" HorizontalAlignment="Right" Foreground="Gray"/>
                    </Grid>

                </StackPanel>
            </Border>
        </ScrollViewer>
    </Grid>
</UserControl>